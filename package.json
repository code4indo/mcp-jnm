{
  "name": "mcp-jnm",
  "version": "1.0.6",
  "description": "Interactive Web Dashboard & CLI for Model Context Protocol (MCP) servers including Cloudflare Sandbox Container & SSH MCP",
  "main": "dist/server.cjs",
  "type": "module",
  "bin": {
    "mcp": "dist/cli.cjs"
  },
  "files": [
    "dist",
    "bin",
    "cli.ts",
    "README.md",
    "package.json"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "cloudflare",
    "sandbox-container",
    "ssh",
    "dashboard",
    "cli"
  ],
  "scripts": {
    "dev": "tsx server.ts",
    "build": "vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs && esbuild cli.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/cli.cjs --banner:js=\"#!/usr/bin/env node\"",
    "start": "node dist/server.cjs",
    "clean": "rm -rf dist server.js",
    "lint": "tsc --noEmit",
    "cli": "tsx cli.ts"
  },
  "dependencies": {
    "@google/genai": "^2.17.1",
    "@modelcontextprotocol/sdk": "1.30.0",
    "@tailwindcss/vite": "^4.3.3",
    "@vitejs/plugin-react": "^5.2.0",
    "commander": "^15.0.0",
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "lucide-react": "^0.577.0",
    "motion": "^12.43.0",
    "picocolors": "^1.1.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "vite": "^6.4.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^22.20.1",
    "autoprefixer": "^10.5.4",
    "esbuild": "^0.28.2",
    "tailwindcss": "^4.3.3",
    "tsx": "^4.23.12",
    "typescript": "~5.9.3"
  }
}
